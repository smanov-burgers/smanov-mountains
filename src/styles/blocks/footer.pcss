.footer {
    background-color: #252830;
    max-height: 407px;
    display: flex;
    padding: 105px 160px;
    padding-top: 95px;
    justify-content: space-between;
    align-items: flex-start;
    @include desktop {
        padding-left:4px;
    }
}

.footer__left {
    display: flex;
    flex-wrap: wrap;
    flex:1;
}

.footer__right {
    display: flex;
    flex-wrap: wrap;
    flex:1;
}

.footer__copyright {
    display: flex;
    @include osans();
    color: rgba(#fff, 0.1);
    font-size: 48px;
    font-weight: 700;
    line-height: 48px;
    flex-shrink: 0;
    padding: 0 60px;
    padding-right: 50px;
}

.footer__author {
    display: flex;
    flex-wrap: wrap;
}

.footer__author__name {
    @include osans();
    color: #fff;
    font-size: 18px;
    font-weight: 700;
    line-height: 30px;
    text-transform: uppercase;
}

.footer__author__desc {
    @include osans();
    color: rgba(#fff, 0.15);
    font-size: 16px;
    font-weight: 400;
    line-height: 30px;
    padding-top: 18px;
    min-width: 144px;
}

.footer__ps {
    @include osans();
    color: rgba(#fff, 0.15);
    font-size: 16px;
    font-weight: 400;
    line-height: 30px;
}

.footer__misc {
    display: flex;
    flex-wrap: wrap;
    padding-left: 138px;
}

.footer__nav {
    width: 100%;
    padding-bottom: 56px;
}

.footer__misc-left {
    display: flex;
    flex-wrap: wrap;
    flex:1;
}

.footer__misc-right {
    display: flex;
    flex-wrap: wrap;
    flex:1;
}

.footer__quote {
    @include osans();
    color: rgba(#fff, 0.15);
    font-size: 16px;
    font-weight: 400;
    line-height: 30px;
}

.footer__quote-author {
    @include osans();
    color: #fff;
    font-size: 16px;
    font-weight: 400;
    line-height: 30px;
    font-style: italic;
}

.footer__socials {
    padding-top: 22px;
}

.footer__quote-author{
    padding-top: 48px;
}